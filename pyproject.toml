[project]
name = "ai-alarm-sound-recognition"
version = "0.1.0"
description = ""
authors = [
    {name = "JamesMcGarr",email = "james@jamesmcgarr.com"}
]
requires-python = ">=3.13"
dependencies = [
    "sounddevice (>=0.5.5,<0.6.0)",
    "numpy (>=2.4.2,<3.0.0)",
    "torch (>=2.2.0)",
    "torchaudio (>=2.2.0)",
    "scipy (>=1.13.0)"
]

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)",
    "flake8 (>=7.3.0,<8.0.0)"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
